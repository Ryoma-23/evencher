<%= form_with model: [event, event_comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '2', placeholder: "感想をここに", class: "form-control" %>
  <div class="text-right"><%= f.submit "送信", class: "btn btn-info rounded-pill" %></div>
<% end %>